<%= content_for :title, "Edit #{@tag.display_name}" %>

<div class="d-flex justify-content-between align-items-center mb-4">
  <h1 class="h3 mb-0">Edit Tag</h1>
</div>

<div class="card shadow-sm">
  <div class="card-body">
    <%= render "form", tag: @tag %>
  </div>
</div>